config:
  slack:
    webhook: ((slack-webhook))
  github:
    private-key: ((github-private-key))
  cluster:
    worker-count: ((worker_count))
    windows-pool: false
  gcp:
    service-account-json: ((gcp-service-account-json))
    region: ((gcp-region))
    zone: ((gcp-zone))
    cluster-name: ((gcp-cluster-name))
  eirini:
    repo-key: ((eirini-repo-key))
    branch: ((eirini-branch))
  eirini-ci:
    branch: ((ci-resources-branch))
  eirini-release:
    branch: ((eirini-release-branch))
    repo-key: ((eirini-release-repo-key))
  dockerhub:
    user: ((dockerhub-user))
    password: ((dockerhub-password))
  wiremock:
    keystore-pass: ((wiremock-keystore-pass))
  cloud_controller_ng:
    branch: ((cloud-controller-ng-branch))
