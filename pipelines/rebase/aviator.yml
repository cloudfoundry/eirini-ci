spruce:
- base: $SCRIPT_DIR/pipeline.yml
  merge:
  - with:
      files:
      - $PROJECT_ROOT/notifications/slack/hook.yml
      - $PROJECT_ROOT/notifications/slack/rebase-cc.yml
  to: $SCRIPT_DIR/spruced.yml

