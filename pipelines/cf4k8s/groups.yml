groups:
- name: all
  jobs:
  - generate-cf-for-k8s-values
  - deploy-cf-for-k8s-((world-name))
  - smoke-tests-((world-name))
  - delete-cluster-((world-name))
  - create-cluster-((world-name))
  - prepare-cluster-((world-name))
- name: cf-for-k8s-((world-name))
  jobs:
  - generate-cf-for-k8s-values
  - deploy-cf-for-k8s-((world-name))
  - smoke-tests-((world-name))
- name: cluster-((world-name))
  jobs:
  - delete-cluster-((world-name))
  - create-cluster-((world-name))
  - prepare-cluster-((world-name))
