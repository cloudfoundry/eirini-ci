resources:
- name: (( concat "cluster-" config.cluster.name "-staging-event-ready" ))
  icon: check-decagram
  type: semver
  source:
    branch: events
    driver: git
    file: (( concat config.cluster.name "-event-ready" ))
    initial_version: 0.1.0
    private_key: (( grab config.github.private-key ))
    uri: git@github.com:cloudfoundry/eirini-private-config.git
