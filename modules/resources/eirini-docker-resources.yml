resources:
- name: event-reporter-image
  type: registry-image
  icon: docker
  source:
    repository: eirini/event-reporter
    username: (( grab config.dockerhub.user ))
    password: (( grab config.dockerhub.password ))

- name: metrics-collector-image
  type: registry-image
  icon: docker
  source:
    repository: eirini/metrics-collector
    username: (( grab config.dockerhub.user ))
    password: (( grab config.dockerhub.password ))


- name: opi-image
  type: registry-image
  icon: docker
  source:
    repository: eirini/opi
    username: (( grab config.dockerhub.user ))
    password: (( grab config.dockerhub.password ))

- name: route-collector-image
  type: registry-image
  icon: docker
  source:
    repository: eirini/route-collector
    username: (( grab config.dockerhub.user ))
    password: (( grab config.dockerhub.password ))

- name: route-pod-informer-image
  icon: docker
  type: registry-image
  source:
    repository: eirini/route-pod-informer
    username: (( grab config.dockerhub.user ))
    password: (( grab config.dockerhub.password ))

- name: route-statefulset-informer-image
  type: registry-image
  icon: docker
  source:
    repository: eirini/route-statefulset-informer
    username: (( grab config.dockerhub.user ))
    password: (( grab config.dockerhub.password ))

- name: task-reporter-image
  type: registry-image
  icon: docker
  source:
    repository: eirini/task-reporter
    username: (( grab config.dockerhub.user ))
    password: (( grab config.dockerhub.password ))

- name: eirini-controller-image
  type: registry-image
  icon: docker
  source:
    repository: eirini/eirini-controller
    username: (( grab config.dockerhub.user ))
    password: (( grab config.dockerhub.password ))

- name: instance-index-env-injector-image
  type: registry-image
  icon: docker
  source:
    repository: eirini/instance-index-env-injector
    username: (( grab config.dockerhub.user ))
    password: (( grab config.dockerhub.password ))
