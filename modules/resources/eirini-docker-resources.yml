resources:
- name: event-reporter-image
  type: registry-image
  icon: docker
  source:
    repository: eirini/event-reporter
    username: (( grab config.dockerhub.user ))
    password: (( grab config.dockerhub.password ))
    tag: latest

- name: opi-image
  type: registry-image
  icon: docker
  source:
    repository: eirini/opi
    username: (( grab config.dockerhub.user ))
    password: (( grab config.dockerhub.password ))
    tag: latest

- name: task-reporter-image
  type: registry-image
  icon: docker
  source:
    repository: eirini/task-reporter
    username: (( grab config.dockerhub.user ))
    password: (( grab config.dockerhub.password ))
    tag: latest

- name: eirini-controller-image
  type: registry-image
  icon: docker
  source:
    repository: eirini/eirini-controller
    username: (( grab config.dockerhub.user ))
    password: (( grab config.dockerhub.password ))
    tag: latest

- name: instance-index-env-injector-image
  type: registry-image
  icon: docker
  source:
    repository: eirini/instance-index-env-injector
    username: (( grab config.dockerhub.user ))
    password: (( grab config.dockerhub.password ))
    tag: latest

- name: migration-image
  type: registry-image
  icon: docker
  source:
    repository: eirini/migration
    username: (( grab config.dockerhub.user ))
    password: (( grab config.dockerhub.password ))
    tag: latest

- name: resource-validator-image
  type: registry-image
  icon: docker
  source:
    repository: eirini/resource-validator
    username: (( grab config.dockerhub.user ))
    password: (( grab config.dockerhub.password ))
    tag: latest
