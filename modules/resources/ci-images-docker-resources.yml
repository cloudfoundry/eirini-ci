resources:
- name: ci-image
  type: docker-image
  icon: docker
  source:
    repository: eirini/ci
    username: (( grab config.dockerhub.user ))
    password: (( grab config.dockerhub.password ))

- name: telepresence-image
  type: docker-image
  icon: docker
  source:
    repository: eirini/telepresence
    username: (( grab config.dockerhub.user ))
    password: (( grab config.dockerhub.password ))

- name: ibmcloud-image
  type: docker-image
  icon: docker
  source:
    repository: eirini/ibmcloud
    username: (( grab config.dockerhub.user ))
    password: (( grab config.dockerhub.password ))

- name: gcloud-image
  type: docker-image
  icon: docker
  source:
    repository: eirini/gcloud
    username: (( grab config.dockerhub.user ))
    password: (( grab config.dockerhub.password ))

- name: buildah-image
  type: docker-image
  icon: docker
  source:
    repository: eirini/buildah
    username: (( grab config.dockerhub.user ))
    password: (( grab config.dockerhub.password ))

- name: staging-integration-image
  type: docker-image
  icon: docker
  source:
    repository: eirini/staging-integration
    username: (( grab config.dockerhub.user ))
    password: (( grab config.dockerhub.password ))
