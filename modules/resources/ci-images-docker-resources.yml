resources:
- name: ci-image
  type: docker-image
  icon: docker
  source:
    repository: eirini/ci
    username: (( grab config.dockerhub.user ))
    password: (( grab config.dockerhub.password ))

- name: telepresence-image
  type: docker-image
  icon: docker
  source:
    repository: eirini/telepresence
    username: (( grab config.dockerhub.user ))
    password: (( grab config.dockerhub.password ))

- name: gcloud-image
  type: docker-image
  icon: docker
  source:
    repository: eirini/gcloud
    username: (( grab config.dockerhub.user ))
    password: (( grab config.dockerhub.password ))

- name: capi-integration-image
  type: docker-image
  icon: docker
  source:
    repository: eirini/capi-integration
    username: (( grab config.dockerhub.user ))
    password: (( grab config.dockerhub.password ))
