resources:
- name: eirinifs-release
  check_every: 2h
  type: github-release
  source:
    owner: cloudfoundry-incubator
    repository: eirinifs
    access_token: (( grab config.github.access-token ))
