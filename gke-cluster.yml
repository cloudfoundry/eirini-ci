bits:
  env:
    DOMAIN: 35.195.76.44.nip.io
  kube:
    external_ips: []
  secrets:
    BITS_SERVICE_SECRET: bits-secret
    BITS_SERVICE_SIGNING_USER_PASSWORD: bits-secret
    BLOBSTORE_PASSWORD: bits-secret
  services:
    loadbalanced: true

env:
    DOMAIN: 35.195.76.44.nip.io
    UAA_HOST: uaa.35.195.76.44.nip.io
    UAA_PORT: 2793
    ENABLE_OPI_STAGING: true

kube:
    external_ips: []
    storage_class:
      persistent: "standard"
      shared: "standard"
    auth: rbac

secrets:
    CLUSTER_ADMIN_PASSWORD: kobe
    UAA_ADMIN_CLIENT_SECRET: kobe
    BLOBSTORE_PASSWORD: bits-secret

services:
  loadbalanced: true

sizing:
  diego_cell:
    count: 0

eirini:
  secrets:
    BLOBSTORE_PASSWORD: bits-secret
  kube:
    external_ips: []
  services:
    loadbalanced: true
