#!/bin/bash

is-container-ready() {
  declare -r namespace="${1:?Namespace not provided}"
  declare -r resource_name="${2:?Resource name not provided}"
  kubectl get pods "$resource_name" -n "$namespace" -o jsonpath='{.status.containerStatuses[0].ready}'
  #  echo
}
