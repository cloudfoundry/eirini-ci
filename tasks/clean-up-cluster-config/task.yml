platform: linux

image_resource:
  type: docker-image
  source:
    repository: eirini/ibmcloud

inputs:
  - name: ci-resources
  - name: state

outputs:
  - name: state-modified

params:
  CLUSTER_NAME:

run:
  path: ci-resources/tasks/clean-up-cluster-config/task.sh
