---
platform: linux

image_resource:
  type: docker-image
  source:
    repository: diegoteam/eirini

inputs:
- name: state
- name: ci-resources

outputs:
- name: configs

run:
  path: ci-resources/tasks/create-eirini-config.yml
