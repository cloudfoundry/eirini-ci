apiVersion: cert-manager.io/v1alpha2
kind: Certificate
metadata:
  name: router-crt
  namespace: cert-manager
spec:
  secretName: router-ingress
  dnsNames:
  - "*.<dnsName>"
  issuerRef:
    name: letsencrypt-dns-issuer
    kind: ClusterIssuer
